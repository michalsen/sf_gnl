sudo: false
language: php

php:
  - 5.6

branches:
  only:
    - master

notifications:
  email: false

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script:
  - php index.php

after_script:
- echo "Test!"
